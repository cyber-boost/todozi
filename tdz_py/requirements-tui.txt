# Minimal requirements for Todozi TUI only
# Install with: pip install --break-system-packages --ignore-installed pygments -r requirements-tui.txt
# 
# Note: If you get Pygments uninstall errors (system package conflict), use:
# pip install --break-system-packages --ignore-installed pygments textual rich watchdog watchfiles requests aiohttp urllib3 aiofiles structlog pydantic pydantic-settings tqdm

# TUI Framework (Required)
textual>=0.19.0
rich>=13.0.0

# File Watching (Optional but recommended)
watchdog>=3.0.0
watchfiles>=0.20.0

# HTTP & Networking (For API features)
requests>=2.31.0
aiohttp>=3.9.0
urllib3>=2.0.0

# Async File Operations
aiofiles>=23.2.0

# Logging
structlog>=24.1.0

# Core Models (Required for todozi modules)
pydantic>=2.6.0
pydantic-settings>=2.3.0

# Utilities
tqdm>=4.66.0

