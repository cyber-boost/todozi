// Todozi N-API Bindings Stub
// This is a placeholder implementation for publishing to npm
// The actual Rust N-API bindings will be built separately

class Todozi {
  constructor() {
    // Placeholder constructor
    this.initialized = false;
  }

  // Placeholder methods that throw errors indicating Rust bindings are needed
  task(action) {
    throw new Error('Rust N-API bindings not built. Run: cd ../tdz && cargo build --release --features nodejs');
  }

  urgent(action) {
    throw new Error('Rust N-API bindings not built. Run: cd ../tdz && cargo build --release --features nodejs');
  }

  high(action) {
    throw new Error('Rust N-API bindings not built. Run: cd ../tdz && cargo build --release --features nodejs');
  }

  low(action) {
    throw new Error('Rust N-API bindings not built. Run: cd ../tdz && cargo build --release --features nodejs');
  }

  find(query) {
    throw new Error('Rust N-API bindings not built. Run: cd ../tdz && cargo build --release --features nodejs');
  }

  aiFind(query) {
    throw new Error('Rust N-API bindings not built. Run: cd ../tdz && cargo build --release --features nodejs');
  }

  done(taskId) {
    throw new Error('Rust N-API bindings not built. Run: cd ../tdz && cargo build --release --features nodejs');
  }

  start(taskId) {
    throw new Error('Rust N-API bindings not built. Run: cd ../tdz && cargo build --release --features nodejs');
  }

  all() {
    throw new Error('Rust N-API bindings not built. Run: cd ../tdz && cargo build --release --features nodejs');
  }

  remember(moment, meaning) {
    throw new Error('Rust N-API bindings not built. Run: cd ../tdz && cargo build --release --features nodejs');
  }

  idea(idea) {
    throw new Error('Rust N-API bindings not built. Run: cd ../tdz && cargo build --release --features nodejs');
  }

  stats() {
    throw new Error('Rust N-API bindings not built. Run: cd ../tdz && cargo build --release --features nodejs');
  }

  // Add more placeholder methods as needed...
}

module.exports = { Todozi };
